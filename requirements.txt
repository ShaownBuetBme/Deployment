# Python dependencies for local and CI execution

#start code here
# TODO: add web framework (fastapi or flask)
# TODO: add server runtime (uvicorn if using FastAPI)
# TODO: add test framework (pytest)
# TODO: add optional linting tools (ruff/flake8)

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pytest>=7.4.3
pydantic>=2.5.0
httpx >=0.25.0